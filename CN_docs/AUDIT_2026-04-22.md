# Audit ArtisaStock — 2026-04-22
> Pipeline multi-agents (#1 PO → #7 DevOps) — basé sur relecture de l'audit 2026-04-21 + code source post-sprints P0→P3
> Date : 2026-04-22
> Périmètre : Application C# WinForms + MySQL + DevOps Docker

---

## Scores par agent

| Agent         | Domaine                     | Score 2026-04-22 | Score 2026-04-21 | Évolution |
| ------------- | --------------------------- | ---------------- | ---------------- | --------- |
| #1 PO         | Fonctionnel & Produit       | **~80% US**      | 6.0/10           | ↑ Progressions confirmées, TICKET-15/22/23 non livrés |
| #2 Architect  | Architecture & Design       | **7.2/10**       | 7.0/10           | ↑ +0.2 — FrmListeBase<T> encode ShowDialog(), SQL désync |
| #3 UI/UX      | Ergonomie WinForms          | **7.4/10**       | 6.8/10           | ↑ +0.6 — AppColors créé, état vide, hint, catch loggués |
| #4 Backend    | DAL, Sécurité & Algorithmes | **8.0/10**       | 7.5/10           | ↑ +0.5 — TOCTOU réduit, réservations, anti-cycle |
| #5 Frontend   | Code Forms & Patterns       | **7.0/10**       | 6.0/10           | ↑ +1.0 — 5 Forms migrées FrmListeBase<T>, AppColors |
| #6 QA         | Qualité & Robustesse        | **6.0/10**       | 4.5/10           | ↑ +1.5 — R1/R2/R5/R8/R9 fermés, R7 toujours actif |
| #7 DevOps     | Infrastructure & Sécurité   | **4.5/10**       | N/A              | Nouveau — critique, .env/.config en git |

**Score global estimé : 7.2 / 10** (vs 6.3/10 le 2026-04-21)

---

## Tableau de bord — Tickets urgents (P0 transversal)

| ID | Description | Agent(s) | Sévérité |
|----|-------------|----------|----------|
| **SEC-1** | `App.config` versionné avec `root/root` en clair — credentials DB exposés | DevOps | CRITIQUE |
| **SEC-2** | `site-laravel/.env` en git avec `APP_KEY` réelle malgré `.gitignore` | DevOps/QA | CRITIQUE |
| **SEC-3** | Credentials `#if DEBUG` en clair dans `FrmLogin.cs:20-23` | Frontend/QA | HAUTE |
| **ARCH-1** | `FrmListeBase<T>.OnAjouter/OnModifier` encode `ShowDialog()` — standardise la violation SFA au lieu de la corriger | Architect/Frontend | HAUTE |
| **INFRA-1** | Ordre migrations Docker cassé — `v10-v13` chargés avant `v4-v9` (tri alphabétique) | DevOps | HAUTE |
| **FUNC-1** | TICKET-15 non livré — `FrmIngredientEdit` et `FrmBomFicheEdit` n'héritent pas `FrmEditBase` | PO/Frontend | HAUTE |
| **UX-1** | `btnDel` `Size(24,22)` violation Fitts — inchangé malgré sprints | UI/UX | HAUTE |
| **UX-2** | Palette dupliquée dans `FrmPrincipal` — AppColors ignoré dans le fichier le plus volumineux | UI/UX/Architect | MOYENNE |
| **DOC-1** | Commentaire SFA trompeur `FrmPrincipal` ligne 15 — donne une fausse image de l'architecture | PO | MOYENNE |
| **DB-1** | `create_database.sql` désynchronisé — représente schéma v1 (~50 tables) vs réalité post-v13 (~15 tables) | QA/DevOps/Architect | MOYENNE |

---

## AGENT #1 — PRODUCT OWNER : Audit Fonctionnel (2026-04-22)

### Score PO : ~80% US livrées (↑ depuis 6.0/10)

### Progressions confirmées

- **AppColors.cs** créé et utilisé dans les nouveaux fichiers — design system enfin externalisé.
- **5 Forms migrées** vers `FrmListeBase<T>` (FrmBomContextes, FrmBomFiches, FrmBomNiveaux, FrmAchats, FrmIngredients).
- **TICKET-19/20** livrés — état vide explicite dans FrmBomFiches, hint sous `btnLancerProduction`.
- **Catches silencieux** remplacés par `Trace.TraceError()` dans `ShowHubScreen()` et `FrmBomProductionSimulation`.
- **btnEnregistrer conditionnel** — désactivé si `cboInput.Items.Count == 0`.
- **`lblActiviteValeur`** remplace le `cboActivite` grisé dans `FrmBomFicheEdit`.

### Problèmes persistants

| Ticket | Problème | Priorité |
|--------|----------|----------|
| **TICKET-15** | `FrmIngredientEdit` et `FrmBomFicheEdit` n'héritent pas `FrmEditBase` — classes de base non adoptées | HAUTE |
| **TICKET-22** | CRUD BOM en `ShowDialog()` non migré en SFA inline dans `FrmPrincipal` | HAUTE |
| **TICKET-23** | Artefacts orphelins non supprimés (`FrmArtisaStock`, `FrmBomProduction`, `FrmRecettes`, DAL orphelins) | HAUTE |
| — | Commentaire SFA trompeur ligne 15 de `FrmPrincipal` — décrit une architecture que le code ne respecte pas | MOYENNE |
| — | `btnDel` `Size(24,22)` — violation Fitts persistante, inchangée depuis l'audit 2026-04-21 | HAUTE |
| — | `FrmActivites` ouvre toujours en `ShowDialog()` × 3 boutons | HAUTE |
| — | `FrmBomContextes → FrmBomNiveaux` : appel en `ShowDialog()` au lieu de navigation SFA | HAUTE |

### Recommandations PO

1. Retirer le commentaire SFA trompeur `FrmPrincipal:15` — effort 2 minutes, risque examen élevé.
2. Livrer TICKET-15 : faire hériter `FrmIngredientEdit` et `FrmBomFicheEdit` de `FrmEditBase` (estimé 2h).
3. Migrer `FrmActivites` vers navigation SFA inline — `ShowRessourceScreen(RessourceType.Activites)`.
4. Supprimer les artefacts orphelins (TICKET-23) — évite la confusion lors de la démonstration examen.

---

## AGENT #2 — ARCHITECTE : Audit Architecture & Design (2026-04-22)

### Score Architecture : 7.2/10 (↑ depuis 7.0/10)

### Problèmes identifiés

| # | Problème | Sévérité | Fichier(s) | Recommandation |
|---|----------|----------|------------|----------------|
| **P1** | `FrmListeBase<T>` encode `ShowDialog()` dans `OnAjouter/OnModifier` — contradiction SFA fondamentale. La migration standardise la violation au lieu de la corriger. | ÉLEVÉ | `Forms/FrmListeBase.cs` | Remplacer `ShowDialog()` par un callback `Action<T>` injectable ou un événement `DemandeCreation` |
| **P2** | `create_database.sql` désynchronisé — représente état v1 vs réalité post-v13 | ÉLEVÉ | `sql/create_database.sql` | Régénérer depuis dump MySQL complet après toutes migrations |
| **P3** | `bom_fiches.nom` contrainte `UNIQUE` globale incohérente — devrait être `UNIQUE(nom, id_niveau)` | ÉLEVÉ | SQL | Migration `ALTER TABLE bom_fiches DROP INDEX uk_nom, ADD UNIQUE KEY uk_nom_niveau (nom, id_niveau)` |
| **P4** | `fiches_ingredients ON DELETE CASCADE` agressif — supprime des données liées sans avertissement | MOYEN | SQL | Passer en `ON DELETE RESTRICT`, gérer la cascade côté DAL avec transaction explicite |
| **P5** | Index manquant `bom_reservations.actif` — requêtes FIFO filtrent sur `actif=1` sans index | MOYEN | SQL | `ALTER TABLE bom_reservations ADD INDEX idx_actif (actif)` |
| **P6** | DAL orphelins compilés (`RecetteDAL`, `CategorieDAL`, `ParfumDAL`, `ProduitDAL`) — code mort actif | MOYEN | `DAL/` | Supprimer ou `[Obsolete]` |
| **P7** | `SimulationService` couvre seulement 1/3 des opérations async — `Executer` et `CalculerCout` toujours appelés directement depuis les Forms | MOYEN | `Services/` | Compléter la couche service |
| **P8** | `GreenOk` RGB diverge entre `AppColors` et `FrmPrincipal` — deux valeurs différentes pour la même couleur sémantique | FAIBLE | `Forms/AppColors.cs`, `Forms/FrmPrincipal.cs` | Remplacer la valeur inline de `FrmPrincipal` par `AppColors.GreenOk` |
| **P9** | TOCTOU résiduel — `VerifierDisponibilite` déplacé dans la transaction mais sur connexion séparée | MOYEN | `DAL/BomProductionDAL.cs:238` | Fusionner sur la connexion transactionnelle unique |
| **P10** | `GetIdNiveauDeFiche()` ouvre une connexion séparée à l'intérieur d'une transaction | MOYEN | `DAL/BomProductionDAL.cs` | Passer la connexion+transaction en paramètre |

### Points forts maintenus

- Sécurité SQL irréprochable (100% `AddWithValue`, zéro concaténation).
- BCrypt pour l'authentification — aucun mot de passe en clair en base.
- Transactions atomiques sur `BomFicheDAL`, `BomProductionDAL`, `BomContexteDAL`.
- FIFO correctement implémenté avec déduction des réservations.
- `UnitConvertisseur` robuste et centralisé (après fix null-guards).

---

## AGENT #3 — UI/UX DESIGNER : Audit Ergonomie (2026-04-22)

### Score UX/UI : 7.4/10 (↑ depuis 6.8/10)

### Progressions confirmées

- `AppColors.cs` créé — palette centralisée disponible pour toutes les Forms.
- `lblActiviteValeur` remplace le `cboActivite` grisé — Nielsen #8 corrigé dans `FrmBomFicheEdit`.
- Hint textuel sous `btnLancerProduction` quand grisé — Nielsen #1 corrigé.
- État vide explicite dans `FrmBomFiches` — `_lblEtatVide` visible si DGV vide.
- `btnEnregistrer` conditionnel désactivé si aucun input disponible — Nielsen #5 corrigé.
- Catches loggués dans `ShowHubScreen()` et `FrmBomProductionSimulation`.

### Problèmes persistants

| # | Loi / Heuristique | Problème | Fichier:ligne | Correction |
|---|-------------------|----------|---------------|------------|
| **F1** | Fitts | `btnDel` `Size(24,22)` — inchangé malgré audit P0 précédent | `Forms/FrmListeBase.cs` | Minimum `Size(32,28)` |
| **F2** | φ Layout | `SplitContainer` 50/50 — ratio φ (62/38) toujours non respecté | `Forms/FrmPrincipal.cs` | `SplitterDistance` à ~62% de la largeur |
| **F3** | AppColors | Palette dupliquée dans `FrmPrincipal` — fichier le plus volumineux ignore `AppColors` | `Forms/FrmPrincipal.cs` | Remplacer toutes les constantes couleur inline par `AppColors.*` |
| **F4** | SFA | `ShowDialog()` × 6 points encore actifs : Activités, Contextes, Niveaux, Fiches BOM, FrmIngredients depuis cards, FrmBomFiches depuis cards | Multiples | Migration inline SFA |
| **F5** | AppColors | `GreenOk` RGB diverge entre `AppColors` et `FrmPrincipal` | `Forms/FrmPrincipal.cs` | Utiliser `AppColors.GreenOk` |
| **F6** | Grille 8px | ~40% des contrôles hors grille 8px | `Forms/FrmBomFicheEdit.Designer.cs` | Aligner Y sur multiples de 8 |
| **F7** | Nielsen #1 | Volet droit `_dgvStock` sans label titre — l'utilisateur ne sait pas ce qu'il voit | `Forms/FrmPrincipal.cs` | Ajouter `Label` "Stock disponible" en entête du volet |
| **F8** | Fitts | `btnGererAct` / `btnNouvAct` `Size(90/80, 22)` — hauteur 22px sous le seuil 28px | `Forms/FrmPrincipal.cs` | `Size(90, 28)` minimum |
| **F9** | Nielsen #5 | `MessageBox` suppression Niveau ne mentionne pas les fiches liées qui seront supprimées | `Forms/FrmBomNiveaux.cs` | Ajouter "Toutes les fiches de ce niveau seront supprimées" |

### Recommandations UI/UX priorisées

| Priorité | Recommandation | Effort |
|----------|----------------|--------|
| P0 | Migrer palette `FrmPrincipal` vers `AppColors.*` | 30min |
| P0 | `btnDel` → `Size(32,28)` minimum dans `FrmListeBase` | 5min |
| P1 | Corriger ratio φ SplitContainer | 5min |
| P1 | Ajouter titre volet droit `_dgvStock` | 10min |
| P1 | `btnGererAct`/`btnNouvAct` → hauteur 28px | 5min |
| P2 | Message suppression Niveau avec mention fiches liées | 10min |
| P2 | Aligner grille 8px `FrmBomFicheEdit` | 20min |

---

## AGENT #4 — BACKEND ENGINEER : Audit DAL, Sécurité & Algorithmes (2026-04-22)

### Score Backend : 8.0/10 (↑ depuis 7.5/10)

### Progressions confirmées

- **TICKET-01** — `VerifierDisponibilite()` déplacé dans la transaction (TOCTOU réduit).
- **TICKET-08** — Réservations libérées (`actif=0`) dans `ConsumeStock` après chaque décrémentation.
- **TICKET-09** — Anti-cycle `HashSet<int>` dans `BomCoutDAL.CalculerCout()` — `StackOverflowException` éliminé.
- **TICKET-02** — Contrainte `CHECK (quantite_disponible >= 0)` ajoutée en migration v13.
- **Null-guards** ajoutés dans `BomProductionDAL.Executer()` et `UnitConvertisseur.Convertir()`.
- **BCrypt** correct — aucun mot de passe en clair en base.
- **FIFO** correct — tri `date_achat ASC` respecté.
- **Transactions atomiques** maintenues sur toutes les opérations critiques.
- **Zéro injection SQL** — 100% `AddWithValue`.

### Problèmes résiduels

| # | Problème | Fichier:ligne | Sévérité | Recommandation |
|---|----------|---------------|----------|----------------|
| **B1** | TOCTOU résiduel — `VerifierDisponibilite` déplacé dans la transaction mais sur une connexion séparée (isolation non garantie) | `BomProductionDAL.cs:238` | MOYEN | Passer la connexion+transaction en paramètre à `VerifierDisponibilite` |
| **B2** | `GetIdNiveauDeFiche()` ouvre une connexion séparée à l'intérieur d'une transaction ouverte | `BomProductionDAL.cs` | MOYEN | Calculer `idNiveauDeFiche` avant `BeginTransaction` ou passer `conn`+`tx` |
| **B3** | `ActiviteDAL.Desactiver()` sans transaction — opération non atomique (TOCTOU mineur) | `DAL/ActiviteDAL.cs` | FAIBLE | Encapsuler dans `BeginTransaction` |
| **B4** | Réutilisation de `cmd` avec `Parameters.Clear()` dans `ActiviteDAL` — pattern fragile | `DAL/ActiviteDAL.cs:100-122` | FAIBLE | Créer une commande distincte par opération SQL |
| **B5** | `BomFicheDAL.InsertLignes` déclaré `internal` au lieu de `private` — namespace DAL peut le contourner | `DAL/BomFicheDAL.cs:209` | FAIBLE | Passer en `private` |
| **B6** | N+1 dans `BomCoutDAL` — 1 SELECT par ligne de type "fiche" | `DAL/BomCoutDAL.cs:127` | MOYEN | Cache `Dictionary<int, BomFiche>` en paramètre de la récursion |
| **B7** | `Services/` incomplète — `Executer` et `CalculerCout` appelés directement depuis les Forms | `Services/SimulationService.cs` | MOYEN | Étendre `SimulationService` pour couvrir les 3 opérations principales |

---

## AGENT #5 — FRONTEND ENGINEER : Audit Code Forms & Patterns (2026-04-22)

### Score Frontend : 7.0/10 (↑ +1.0 depuis 6.0/10)

### Progressions confirmées

- **5 Forms** migrées vers `FrmListeBase<T>` : `FrmBomContextes`, `FrmBomFiches`, `FrmBomNiveaux`, `FrmAchats`, `FrmIngredients` (vs 1 avant).
- `AppColors` utilisé dans `FrmListeBase`, `FrmBomProductionSimulation`, `FrmBomFicheEdit`.
- `EmbedForm()` fonctionnel — pattern SFA de base opérationnel.
- TICKET-19/20 livrés — état vide, hint production.
- `FrmIngredients` — cas modèle exemplaire de migration.

### Problèmes persistants

| # | Problème | Fichier | Sévérité |
|---|----------|---------|----------|
| **FE1** | `FrmListeBase<T>` encode `ShowDialog()` dans `OnAjouter/OnModifier` — migration standardise le modal au lieu de le corriger | `Forms/FrmListeBase.cs` | HAUTE |
| **FE2** | `FrmBomContextes → FrmBomNiveaux` en `ShowDialog()` | `Forms/FrmBomContextes.cs` | HAUTE |
| **FE3** | Palette dupliquée dans `FrmPrincipal` — fichier le plus volumineux ignore `AppColors` | `Forms/FrmPrincipal.cs` | MOYENNE |
| **FE4** | TICKET-15 non livré — `FrmIngredientEdit` et `FrmBomFicheEdit` n'héritent pas `FrmEditBase` | `Forms/FrmIngredientEdit.cs`, `Forms/FrmBomFicheEdit.cs` | HAUTE |
| **FE5** | `StateChanged` déclaré dans `AppState` mais jamais abonné — dead event | `Navigation/AppState.cs` | FAIBLE |
| **FE6** | `EmbedForm()` sans `try/finally` autour de `SuspendLayout` — si exception entre Suspend et Resume, UI figée | `Forms/FrmPrincipal.cs` | MOYEN |
| **FE7** | `Controls.Clear()` sans `Dispose()` des contrôles enfants — memory leak potentiel | `Forms/FrmPrincipal.cs` | MOYEN |
| **FE8** | `#if DEBUG` credentials en clair `FrmLogin:20-23` | `Forms/FrmLogin.cs:20-23` | HAUTE |
| **FE9** | Null-guard manquant sur `niveau` dans `FrmBomFicheEdit` constructeur | `Forms/FrmBomFicheEdit.cs` | MOYEN |
| **FE10** | `nudQteLigne.Minimum = 0` — saisie de quantité nulle possible dans une ligne de fiche BOM | `Forms/FrmBomFicheEdit.cs` | MOYEN |
| **FE11** | `InputItem.TypeInput` de type `string` au lieu d'`enum` — risque de faute de frappe silencieuse | `Models/InputItem.cs` | FAIBLE |

### Recommandations Frontend priorisées

| Priorité | Recommandation | Effort |
|----------|----------------|--------|
| P0 | Supprimer `#if DEBUG` credentials `FrmLogin:20-23` | 5min |
| P0 | Livrer TICKET-15 — héritages `FrmEditBase` | 2h |
| P1 | `EmbedForm()` → `try/finally` autour `SuspendLayout` | 15min |
| P1 | `Controls.Clear()` → `Controls.OfType<Form>().ToList().ForEach(f => f.Dispose())` | 10min |
| P1 | `nudQteLigne.Minimum = 1` dans `FrmBomFicheEdit` | 2min |
| P1 | Migrer palette `FrmPrincipal` vers `AppColors` | 30min |
| P2 | `InputItem.TypeInput` → enum `TypeInputBOM { Ingredient, Fiche }` | 20min |
| P2 | Null-guard `niveau` dans `FrmBomFicheEdit` constructeur | 5min |
| P2 | Supprimer dead event `StateChanged` ou l'abonner | 10min |

---

## AGENT #6 — QA ENGINEER : Audit Qualité & Robustesse (2026-04-22)

### Score QA : 6.0/10 (↑ depuis 4.5/10)

### Tickets fermés depuis 2026-04-21

| Ticket | Description | Statut |
|--------|-------------|--------|
| **R1** | TOCTOU `VerifierDisponibilite` hors transaction | FERMÉ (déplacé dans transaction) |
| **R2** | Null-guards `fiche` et `niveau` dans `Executer()` | FERMÉ |
| **R5** | `CHECK (quantite_disponible >= 0)` manquant | FERMÉ (migration v13) |
| **R8** | `UNIQUE nom` global vs `NomExiste` par niveau | FERMÉ (contrainte alignée) |
| **R9** | Réservations orphelines non libérées | FERMÉ (libération dans `ConsumeStock`) |

### Ticket toujours actif

| Ticket | Description | Fichier | Sévérité |
|--------|-------------|---------|----------|
| **R7** | `create_database.sql` désynchronisé — schéma v1 vs réalité post-v13 | `sql/create_database.sql` | HAUTE |

### Nouveaux bugs identifiés (sprint P0→P3)

| ID | Bug | Fichier | Sévérité |
|----|-----|---------|----------|
| **NB1** | `ActiviteDAL` réutilise `cmd` avec `Parameters.Clear()` — pattern fragile | `DAL/ActiviteDAL.cs:100-122` | MOYEN |
| **NB2** | `GetIdNiveauDeFiche()` connexion séparée à l'intérieur d'une transaction | `DAL/BomProductionDAL.cs` | MOYEN |
| **NB3** | `InsertLignes` déclaré `internal` au lieu de `private` | `DAL/BomFicheDAL.cs:209` | FAIBLE |

### Lacunes plan de tests

- Pas de test de production FIFO multi-lots (scénario : 3 lots, consommation partielle).
- Pas de test de conversion d'unités inter-groupes (masse → volume : doit échouer).
- Pas de test de stock partiel (disponible < requis → `BomManque.Manque > 0`).
- Pas de test de cycle BOM (A → B → A : `HashSet` doit détecter et lever l'exception).
- **0 test automatisé** — couverture 0% sur code métier C#.

### Script de reset DB

`sql/reset_db_for_tests.sql` est utilisable mais **ne doit pas être dans le volume Docker init** — risque d'effacement de données au redémarrage du conteneur.

### Recommandations QA

| Priorité | Recommandation | Type |
|----------|----------------|------|
| P0 | Mettre à jour `create_database.sql` — état réel post-v13 | Documentation/Infra |
| P1 | Créer `CharlesNadejda.Tests` — tests `UnitConvertisseur` + cycle BOM + FIFO | Tests |
| P1 | Corriger NB1 — commande distincte par opération dans `ActiviteDAL` | Bug |
| P1 | Corriger NB2 — passer connexion+tx à `GetIdNiveauDeFiche()` | Bug |
| P2 | Déplacer `reset_db_for_tests.sql` hors du volume Docker init | Infra |
| P2 | Ajouter `CommandTimeout` sur opérations longues | Robustesse |

---

## AGENT #7 — DEVOPS : Audit Infrastructure & Sécurité (2026-04-22)

### Score DevOps : 4.5/10 (nouveau — premier audit)

### Problèmes critiques — Sécurité

| ID | Problème | Fichier | Sévérité |
|----|----------|---------|----------|
| **SEC-1** | `App.config` versionné avec credentials `root/root` en clair — exposés dans l'historique git | `App.config` | CRITIQUE |
| **SEC-2** | `site-laravel/.env` en git malgré `.gitignore` avec `APP_KEY` réelle | `site-laravel/.env` | CRITIQUE |
| **SEC-3** | `APP_DEBUG=true` dans `.env` versionné — stack traces exposées en production | `site-laravel/.env` | HAUTE |
| **SEC-4** | Port MySQL `3306` exposé sur `0.0.0.0` — accessible depuis l'extérieur du réseau Docker | `docker-compose.yml` | HAUTE |
| **SEC-5** | phpMyAdmin sans restriction d'accès IP — interface d'admin DB exposée | `docker-compose.yml` | HAUTE |

### Problèmes d'infrastructure

| ID | Problème | Sévérité |
|----|----------|----------|
| **INFRA-1** | Ordre migrations Docker cassé — `v10-v13` chargés avant `v4-v9` (tri alphabétique de `ls`) | CRITIQUE |
| **INFRA-2** | `reset_db_for_tests.sql` dans le volume `docker-entrypoint-initdb.d` — efface les données au redémarrage | HAUTE |
| **INFRA-3** | `create_database.sql` représente schéma v1 (~50 tables) vs réalité post-v13 (~15 tables) | HAUTE |

### Artefacts orphelins dans le `.csproj`

~25% des fichiers `.csproj` référencent des Forms ou DAL orphelins non supprimés :
- `Form1.cs`, `RecetteDAL.cs`, `CategorieDAL.cs`, `FrmRecettes.cs`, `FrmBomSimulation.cs` compilés mais non utilisés.
- Augmente le temps de compilation, crée de la confusion lors de la navigation dans le projet.

### Recommandations DevOps priorisées

| Priorité | Recommandation | Effort |
|----------|----------------|--------|
| P0 | Purger `App.config` de git — credentials dans variable d'env ou `App.config.local` ignoré | 30min |
| P0 | Purger `site-laravel/.env` de git — ajouter au `.gitignore`, créer `.env.example` | 30min |
| P0 | Corriger ordre migrations Docker — nommer avec préfixe numérique `01_...` à `13_...` | 1h |
| P1 | Restreindre port MySQL à `127.0.0.1:3306` dans `docker-compose.yml` | 5min |
| P1 | Ajouter restriction IP phpMyAdmin (réseau Docker interne uniquement) | 10min |
| P1 | Déplacer `reset_db_for_tests.sql` hors du volume init | 10min |
| P1 | Supprimer `#if DEBUG` credentials `FrmLogin` (voir Frontend FE8) | 5min |
| P2 | Supprimer artefacts orphelins du `.csproj` | 1h |
| P2 | Régénérer `create_database.sql` depuis dump complet post-v13 | 30min |
| P2 | Passer `APP_DEBUG=false` avant toute démo ou partage du `.env` | 2min |

---

## MATRICE DE PRIORITÉS — SYNTHÈSE TRANSVERSALE

### P0 — Avant démo examen (actions < 1h cumulées)

| Action | Agent(s) | Effort |
|--------|----------|--------|
| Purger `App.config` et `.env` de l'historique git | DevOps | 30min |
| Supprimer `#if DEBUG` credentials FrmLogin | Frontend/DevOps | 5min |
| Corriger ordre migrations Docker (nommage numérique) | DevOps | 1h |
| Retirer commentaire SFA trompeur `FrmPrincipal:15` | PO | 2min |
| `btnDel` → `Size(32,28)` minimum dans `FrmListeBase` | UI/UX | 5min |
| Migrer palette `FrmPrincipal` vers `AppColors.*` | UI/UX/Frontend | 30min |

### P1 — Sprint de polish (avant soumission finale)

| Action | Agent(s) | Effort |
|--------|----------|--------|
| Livrer TICKET-15 — héritages `FrmEditBase` | PO/Frontend | 2h |
| Corriger TOCTOU résiduel — connexion unique dans transaction | Backend/Architect | 1h |
| `EmbedForm()` → `try/finally` autour `SuspendLayout` | Frontend | 15min |
| `Controls.Clear()` → Dispose des Forms enfants | Frontend | 10min |
| `nudQteLigne.Minimum = 1` dans `FrmBomFicheEdit` | Frontend | 2min |
| Restreindre port MySQL + phpMyAdmin dans Docker | DevOps | 15min |
| Déplacer `reset_db_for_tests.sql` hors volume init Docker | DevOps/QA | 10min |
| Mettre à jour `create_database.sql` post-v13 | QA/DevOps | 30min |
| Corriger ratio φ SplitContainer | UI/UX | 5min |
| Supprimer artefacts orphelins (TICKET-23) | PO/Architect | 1h |

### P2 — Pour aller plus loin

| Action | Agent(s) | Effort |
|--------|----------|--------|
| Migrer CRUD BOM de `ShowDialog()` vers SFA inline | PO/Architect/Frontend | 8h |
| Créer `CharlesNadejda.Tests` — tests `UnitConvertisseur` + FIFO + cycle BOM | QA | 2h |
| N+1 `BomCoutDAL` — cache `Dictionary<int, BomFiche>` | Backend | 2h |
| `InputItem.TypeInput` → enum `TypeInputBOM` | Frontend | 20min |
| `FrmListeBase<T>` — callback injectable au lieu de `ShowDialog()` | Architect/Frontend | 4h |
| Index `bom_reservations.actif` | Architect/DB | 10min |
| `SimulationService` étendu pour couvrir `Executer` et `CalculerCout` | Backend | 3h |

---

*Audit généré le 2026-04-22 par l'équipe d'agents IA (#1 PO → #7 DevOps) — basé sur relecture de l'audit 2026-04-21 et du code source post-sprints P0→P3. Aucune hypothèse non vérifiée.*
